# AUR packages — shared across ALL hosts.
# One package per line. Blank lines and #comments are ignored.
# Per-host extras go in  hosts/<hostname>/aur.txt

# Terminal
# Built from git, not the official `wezterm` package
wezterm-git
tmux-git

# Fonts
## Persian font
vazirmatn-fonts

# Hypr ecosystem extras
wlogout

# App launcher: walker + its elephant backend.
# walker is only a frontend — every result it shows comes from the `elephant`
# daemon, and each data source is a SEPARATE package that drops a .so into
# /usr/lib/elephant. Installing `elephant` alone gets you a launcher that finds
# nothing at all, which is exactly how this sat broken for a while.
# Add a provider here to turn it on; see home/dot_config/walker/config.toml.
#   desktopapplications  the app list itself — without it walker is empty
#   calc                 `=` prefix and inline results; pulls in libqalculate
#   websearch            `@` prefix
#   runner               `>` prefix, runs anything in $PATH
#   clipboard            `:` prefix; replaced cliphist. Pulls in imagemagick
#   providerlist         `;` prefix, lists the above
walker-bin
elephant
elephant-desktopapplications
elephant-calc
elephant-websearch
elephant-runner
elephant-clipboard
elephant-providerlist

# waybar-with-cava.
waybar-cava
libcava

# Rootless Docker. `dockerd-rootless-setuptool.sh` is NOT part of Arch's
# `docker` package — it only ships here, and install.sh skips the whole rootless
# setup (leaving Docker unusable) when this is missing.
docker-rootless-extras

# Editors / IDEs / agents
# (neovim itself is official — see shared/pacman.txt.)
claude-code
visual-studio-code-bin
cursor-bin

# Work / social / media
teams-for-linux
slack-desktop
zoom

# Other Apps
jellyfin-desktop
spotify
google-chrome

# Editor tooling (LSP / formatters not in official repos).
hyprls

