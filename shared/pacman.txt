# Official-repo (pacman) packages — shared across ALL hosts.
# One package per line. Blank lines and #comments are ignored.
# Per-host extras go in  hosts/<hostname>/pacman.txt

# Must Have
git
vim
nano
neovim
wget
rsync
btop

# Essentials
psmisc
pacman-contrib
keychain

# Shell + terminal tools
tmux
zsh
fastfetch
zip
unzip
fzf
tree
lsd
ripgrep
bat
fd


# Shell/editor config stack and syntax highlighting
chezmoi
git-delta
zsh-autosuggestions
zsh-syntax-highlighting

# GTK dark theme (referenced by ~/.config/gtk-*/settings.ini)
gnome-themes-extra

# Dev toolchains / Version Managers
gcc
cmake
nodejs
npm
python
rustup
luarocks
uv
postgresql


# LSPs / linters / formatters
ruff
typescript-language-server
lua-language-server
clang
codespell
stylua
# Used by .github/workflows/ci.yml; installed here so the same lint can be run
# locally before pushing:  shellcheck --severity=warning install.sh bin/*.sh
shellcheck

# Wayland / Hyprland core
hyprland
hyprlock
hyprpaper
hypridle
hyprpicker
hyprsunset
hyprshot
hyprpolkitagent
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
qt5-wayland
qt6-wayland
polkit
brightnessctl
grim
slurp
swappy
wl-clipboard

# Desktop shell / utilities
# `waybar` is deliberately NOT here: waybar-cava (packages/aur.txt) declares
# `conflicts=waybar provides=waybar`, and pacman under --noconfirm refuses to
# remove a conflicting installed package, so installing both aborts the AUR
# half of the run. The AUR package is the one that has the cava module.
lm_sensors
mako
libnotify
nwg-look
qt5ct
qt6ct
yazi
# GUI file manager bound to $mainMod+E in hypr/bindings.conf; its config lives in
# home/dot_config/Thunar. gvfs gives it trash, mounting and network browsing.
thunar
thunar-archive-plugin
gvfs
viewnior
zathura
zathura-pdf-mupdf

# Audio
pipewire
pipewire-alsa
pipewire-pulse
pipewire-jack
wireplumber
rtkit
pamixer
pavucontrol
easyeffects
playerctl
pulsemixer

# Bluetooth
bluez
bluez-utils
blueman

# Networking + SSH
networkmanager
network-manager-applet
openssh
fail2ban
openvpn

# Virtualisation
docker
docker-compose
docker-buildx

# Fonts
noto-fonts
noto-fonts-cjk
noto-fonts-emoji
ttf-fira-code
ttf-liberation
# The patched FiraCode Nerd Font. Was an AUR package; now in [extra].
ttf-firacode-nerd
# The standalone Nerd Font glyph set (README calls it "Symbols Nerd Font Mono").
# It is what yazi and the waybar modules fall back to for icons; the patched
# ttf-firacode-nerd in aur.txt only covers the FiraCode family.
ttf-nerd-fonts-symbols-mono
xkeyboard-config

# Icon theme. Was named directly by fuzzel.ini; walker replaced fuzzel and takes
# its icons from the GTK icon theme instead (gtk-*/settings.ini, currently
# Adwaita), so nothing in this repo points at Papirus any more — it is kept
# because it is the nicer set to switch those files to.
papirus-icon-theme

# Session / misc
firefox
dconf
flatpak
gnupg
mtr
# Virtual input device, used by the fusuma gesture setup (see README). Never
# give it sudo — the `input` group membership install.sh adds is enough.
ydotool

# Display manager
greetd
greetd-tuigreet

# Apps
telegram-desktop
signal-desktop
vlc
discord
obsidian
axel
cava
socat
jq
gparted
ntfs-3g
