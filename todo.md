is there a safer way to update packages than -Syu
how to delete unused packages in archlinux. like apt-get autoremove and autoclean in ubuntu
hyprland says it started without start-hyprland and it's highly discouraged
--agents is depricated for ssh-agent in keychain
Check if we can snapshot before `yay -Syu` like in nixos
